@using Mandarin.ViewModels.Index.MandarinMap

<div class="max-w-full md:w-1/2 self-center w-full">
  <iframe src="@ViewModel.MapUri" class="border-0 px-2 w-full" width="@ViewModel.Width" height="@ViewModel.Height" frameborder="0" allowfullscreen=""></iframe>
</div>

@code
{
  /// <summary>
  /// Gets or sets the view model for the map.
  /// </summary>
  [Parameter]
  public IMandarinMapViewModel ViewModel { get; set; }
}
