<hr class="@FullClasses" />

@code
{
  private string FullClasses
    => string.Join(" ", "border-mandarin-gold border-t-2", Class ?? string.Empty);

  [Parameter]
  public string Class { get; set; }
}